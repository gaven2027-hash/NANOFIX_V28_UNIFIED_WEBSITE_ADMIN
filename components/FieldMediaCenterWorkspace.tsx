'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import { Badge } from './Badge';
import { SectionCard } from './SectionCard';
import { FieldMediaAccessControlPanel } from './FieldMediaAccessControlPanel';

type Row = Record<string, unknown>;

const inputClass = 'w-full rounded-2xl border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-800 outline-none transition focus:border-activeBlue focus:ring-2 focus:ring-blue-100';
const labelClass = 'mb-1 block text-xs font-black uppercase tracking-[0.12em] text-slate-500';
const objectTypes = ['customer','lead','service_request','job','engineer_inspection','quotation','invoice','payment','warranty','material','supplier','other'];
const uploadStages = ['customer_before_submit','intake_review','engineer_before','engineer_during','engineer_after','quotation_support','invoice_support','warranty_proof','payment_proof','general'];
const visibilities = ['admin_internal','customer_visible','engineer_visible','public_approved'];
const statuses = ['active','review_required','approved','rejected','archived','deleted'];
const defaultRoles = ['super_admin','content_admin','engineer','operations_admin','finance','support'];

function formatValue(value: unknown) { if (value === null || value === undefined || value === '') return '—'; if (Array.isArray(value)) return value.join(', '); if (typeof value === 'object') return JSON.stringify(value); return String(value); }
function stringList(value: unknown) { return Array.isArray(value) ? value.map(String).filter(Boolean) : []; }
function tone(value: unknown): 'blue' | 'green' | 'amber' | 'red' | 'gray' | 'cyan' { const text = String(value || '').toLowerCase(); if (/(active|approved|customer_visible|engineer_after|warranty|job)/.test(text)) return 'green'; if (/(review|intake|during|quotation|invoice|payment|engineer)/.test(text)) return 'amber'; if (/(rejected|deleted|archived)/.test(text)) return 'red'; if (/(customer|service|lead|media|public)/.test(text)) return 'cyan'; return 'blue'; }
function safeJsonString(value: unknown, fallback: Row = {}) { if (!value) return JSON.stringify(fallback, null, 2); if (typeof value === 'string') return value; return JSON.stringify(value, null, 2); }
function parseJson(value: string) { try { return JSON.parse(value || '{}'); } catch { return null; } }
function roleDefaults(objectType: string) { if (['quotation','invoice','payment'].includes(objectType)) return ['super_admin','operations_admin','finance']; if (['customer','lead','service_request'].includes(objectType)) return ['super_admin','operations_admin','support']; if (['job','engineer_inspection','warranty'].includes(objectType)) return ['super_admin','operations_admin','support']; return ['super_admin','operations_admin']; }

export function FieldMediaCenterWorkspace() {
  const [links, setLinks] = useState<Row[]>([]);
  const [people, setPeople] = useState<Row[]>([]);
  const [roleOptions, setRoleOptions] = useState<string[]>(defaultRoles);
  const [search, setSearch] = useState('');
  const [objectType, setObjectType] = useState('service_request');
  const [objectId, setObjectId] = useState('');
  const [uploadStage, setUploadStage] = useState('general');
  const [visibility, setVisibility] = useState('admin_internal');
  const [status, setStatus] = useState('active');
  const [referenceLabel, setReferenceLabel] = useState('');
  const [description, setDescription] = useState('');
  const [tags, setTags] = useState('');
  const [metadataJson, setMetadataJson] = useState(safeJsonString({ source: 'field_media_center', admin_review_required: true }));
  const [allowedViewRoles, setAllowedViewRoles] = useState<string[]>(roleDefaults('service_request'));
  const [allowedUseRoles, setAllowedUseRoles] = useState<string[]>(roleDefaults('service_request'));
  const [allowedViewActorIds, setAllowedViewActorIds] = useState<string[]>([]);
  const [deniedViewActorIds, setDeniedViewActorIds] = useState<string[]>([]);
  const [allowedUseActorIds, setAllowedUseActorIds] = useState<string[]>([]);
  const [deniedUseActorIds, setDeniedUseActorIds] = useState<string[]>([]);
  const [selectedLink, setSelectedLink] = useState<Row | null>(null);
  const [message, setMessage] = useState('');
  const [loading, setLoading] = useState(false);
  const [saving, setSaving] = useState(false);

  const loadLinks = useCallback(async () => {
    setLoading(true);
    setMessage('');
    const params = new URLSearchParams();
    if (search) params.set('search', search);
    if (objectType) params.set('object_type', objectType);
    if (objectId) params.set('object_id', objectId);
    if (uploadStage) params.set('upload_stage', uploadStage);
    if (visibility) params.set('visibility', visibility);
    if (status) params.set('status', status);
    const response = await fetch(`/api/admin/field-media?${params.toString()}`, { cache: 'no-store' });
    const json = await response.json().catch(() => ({}));
    setLoading(false);
    if (!response.ok || !json.ok) return setMessage(json.error || 'Load failed. / 加载失败。');
    setLinks(json.links || []);
    setPeople(json.people || []);
    setRoleOptions(json.roleOptions || defaultRoles);
  }, [search, objectType, objectId, uploadStage, visibility, status]);

  useEffect(() => { void loadLinks(); }, [loadLinks]);
  useEffect(() => { if (!selectedLink) { const roles = roleDefaults(objectType); setAllowedViewRoles(roles); setAllowedUseRoles(roles); } }, [objectType, selectedLink]);

  function resetForm() { setSelectedLink(null); setReferenceLabel(''); setDescription(''); setTags(''); setMetadataJson(safeJsonString({ source: 'field_media_center', admin_review_required: true })); setStatus('active'); setAllowedViewRoles(roleDefaults(objectType)); setAllowedUseRoles(roleDefaults(objectType)); setAllowedViewActorIds([]); setDeniedViewActorIds([]); setAllowedUseActorIds([]); setDeniedUseActorIds([]); }
  function open(link: Row) { setSelectedLink(link); setObjectType(String(link.object_type || 'service_request')); setObjectId(String(link.object_id || '')); setUploadStage(String(link.upload_stage || 'general')); setVisibility(String(link.visibility || 'admin_internal')); setStatus(String(link.status || 'active')); setReferenceLabel(String(link.reference_label || '')); setDescription(String(link.description || '')); setTags(Array.isArray(link.tags) ? (link.tags as string[]).join(', ') : ''); setMetadataJson(safeJsonString(link.metadata_json || { source: 'field_media_center' })); setAllowedViewRoles(stringList(link.allowed_view_roles)); setAllowedUseRoles(stringList(link.allowed_use_roles)); setAllowedViewActorIds(stringList(link.allowed_view_actor_ids)); setDeniedViewActorIds(stringList(link.denied_view_actor_ids)); setAllowedUseActorIds(stringList(link.allowed_use_actor_ids)); setDeniedUseActorIds(stringList(link.denied_use_actor_ids)); }
  function accessPayload() { return { allowed_view_roles: allowedViewRoles, allowed_use_roles: allowedUseRoles, allowed_view_actor_ids: allowedViewActorIds, denied_view_actor_ids: deniedViewActorIds, allowed_use_actor_ids: allowedUseActorIds, denied_use_actor_ids: deniedUseActorIds, access_policy_json: { role_and_person_acl: true, updated_from: 'field_media_access_panel' } }; }
  async function updateLink(nextStatus?: string) { if (!selectedLink?.link_id) return setMessage('Open a media link first. / 请先打开一条素材关联。'); const metadata = parseJson(metadataJson); if (metadata === null) return setMessage('Metadata JSON is invalid. / Metadata JSON 格式错误。'); setSaving(true); setMessage(''); const response = await fetch('/api/admin/field-media', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ link_id: selectedLink.link_id, object_type: objectType, object_id: objectId || null, upload_stage: uploadStage, visibility, status: nextStatus || status, reference_label: referenceLabel, description, tags, module_key: 'field_operations', usage_context: `${objectType}_${uploadStage}`, ...accessPayload(), metadata_json: metadata }) }); const json = await response.json().catch(() => ({})); setSaving(false); if (!response.ok || !json.ok) return setMessage(json.error || 'Update failed. / 更新失败。'); setMessage('Field media link updated. / 现场素材关联已更新。'); await loadLinks(); open(json.link); }

  const stats = useMemo(() => ({ total: links.length, customerVisible: links.filter((link) => link.visibility === 'customer_visible').length, engineerVisible: links.filter((link) => link.visibility === 'engineer_visible').length, review: links.filter((link) => link.status === 'review_required').length }), [links]);

  return <div className="space-y-5"><SectionCard title="Field Media Center / 现场素材中心" subtitle="Backend-only classification and access control for customer, job, quotation, invoice, payment and warranty media. Client portals cannot read this center directly. / 后台专用现场素材分类与权限管理；客户和工程师门户不能直接读取此中心。"><div className="grid gap-3 md:grid-cols-4"><div className="rounded-2xl bg-blue-50 p-4 ring-1 ring-blue-100"><div className="text-2xl font-black text-blue-900">{stats.total}</div><div className="text-xs font-bold text-blue-700">Total media links / 素材关联</div></div><div className="rounded-2xl bg-emerald-50 p-4 ring-1 ring-emerald-100"><div className="text-2xl font-black text-emerald-900">{stats.customerVisible}</div><div className="text-xs font-bold text-emerald-700">Customer flag / 客户标记</div></div><div className="rounded-2xl bg-cyan-50 p-4 ring-1 ring-cyan-100"><div className="text-2xl font-black text-cyan-900">{stats.engineerVisible}</div><div className="text-xs font-bold text-cyan-700">Engineer flag / 工程师标记</div></div><div className="rounded-2xl bg-amber-50 p-4 ring-1 ring-amber-100"><div className="text-2xl font-black text-amber-900">{stats.review}</div><div className="text-xs font-bold text-amber-700">Review required / 待审核</div></div></div></SectionCard>{message ? <div className="rounded-2xl bg-blue-50 px-4 py-3 text-sm font-bold text-blue-800 ring-1 ring-blue-100">{message}</div> : null}<SectionCard title="Filters / 筛选" subtitle="Search results are filtered by current admin role and person ACL. / 搜索结果会根据当前管理员分组与人员权限过滤。"><div className="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><input className={inputClass} value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search / 搜索" /><select className={inputClass} value={objectType} onChange={(event) => setObjectType(event.target.value)}>{objectTypes.map((item) => <option key={item} value={item}>{item}</option>)}</select><input className={inputClass} value={objectId} onChange={(event) => setObjectId(event.target.value)} placeholder="Object UUID / 对象ID" /><select className={inputClass} value={uploadStage} onChange={(event) => setUploadStage(event.target.value)}>{uploadStages.map((item) => <option key={item} value={item}>{item}</option>)}</select><select className={inputClass} value={visibility} onChange={(event) => setVisibility(event.target.value)}>{visibilities.map((item) => <option key={item} value={item}>{item}</option>)}</select><button type="button" onClick={loadLinks} disabled={loading} className="rounded-2xl bg-slate-900 px-4 py-2 text-sm font-black text-white hover:bg-slate-700 disabled:opacity-60">Search / 搜索</button></div></SectionCard><div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_560px]"><SectionCard title="Linked Field Media / 已关联现场素材" subtitle="Open records to approve, archive or change access control. / 打开记录可审批、归档或调整权限。"><div className="overflow-x-auto rounded-2xl border border-slate-200"><table className="min-w-[980px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-3">Status</th><th className="p-3">Object</th><th className="p-3">Stage</th><th className="p-3">Visibility</th><th className="p-3">Label</th><th className="p-3">Access</th><th className="p-3">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{links.map((link) => <tr key={String(link.link_id)} className="hover:bg-blue-50/50"><td className="p-3"><Badge tone={tone(link.status)}>{formatValue(link.status)}</Badge></td><td className="p-3"><div className="font-black text-slate-800">{formatValue(link.object_type)}</div><div className="font-mono text-xs text-slate-500">{formatValue(link.object_id)}</div></td><td className="p-3"><Badge tone={tone(link.upload_stage)}>{formatValue(link.upload_stage)}</Badge></td><td className="p-3"><Badge tone={tone(link.visibility)}>{formatValue(link.visibility)}</Badge></td><td className="max-w-52 truncate p-3 font-semibold text-slate-700">{formatValue(link.reference_label)}</td><td className="p-3 text-xs font-bold text-slate-500">View: {stringList(link.allowed_view_roles).join(', ') || 'default'}<br />People: {stringList(link.allowed_view_actor_ids).length}</td><td className="p-3"><button type="button" onClick={() => open(link)} className="rounded-xl bg-white px-3 py-2 text-xs font-black text-activeBlue ring-1 ring-blue-100 hover:bg-blue-50">Open / 打开</button></td></tr>)}{!links.length ? <tr><td colSpan={7} className="p-6 text-center text-sm font-bold text-slate-500">{loading ? 'Loading...' : 'No field media links. / 暂无现场素材关联。'}</td></tr> : null}</tbody></table></div></SectionCard><SectionCard title="Access Editor / 权限编辑" subtitle="Edit category, visibility flags, status and exact role/person ACL. / 编辑分类、可见标记、状态和具体角色/人员权限。"><div className="space-y-4"><div className="grid gap-3 md:grid-cols-2"><label><span className={labelClass}>Reference Label / 标签</span><input className={inputClass} value={referenceLabel} onChange={(event) => setReferenceLabel(event.target.value)} /></label><label><span className={labelClass}>Status / 状态</span><select className={inputClass} value={status} onChange={(event) => setStatus(event.target.value)}>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="md:col-span-2"><span className={labelClass}>Description / 描述</span><textarea className={`${inputClass} min-h-20`} value={description} onChange={(event) => setDescription(event.target.value)} /></label><label className="md:col-span-2"><span className={labelClass}>Tags / 标签</span><input className={inputClass} value={tags} onChange={(event) => setTags(event.target.value)} /></label><label className="md:col-span-2"><span className={labelClass}>Metadata JSON / 元数据</span><textarea className={`${inputClass} min-h-28 font-mono text-xs`} value={metadataJson} onChange={(event) => setMetadataJson(event.target.value)} /></label></div><FieldMediaAccessControlPanel people={people} roleOptions={roleOptions} allowedViewRoles={allowedViewRoles} allowedUseRoles={allowedUseRoles} allowedViewActorIds={allowedViewActorIds} deniedViewActorIds={deniedViewActorIds} allowedUseActorIds={allowedUseActorIds} deniedUseActorIds={deniedUseActorIds} onAllowedViewRolesChange={setAllowedViewRoles} onAllowedUseRolesChange={setAllowedUseRoles} onAllowedViewActorIdsChange={setAllowedViewActorIds} onDeniedViewActorIdsChange={setDeniedViewActorIds} onAllowedUseActorIdsChange={setAllowedUseActorIds} onDeniedUseActorIdsChange={setDeniedUseActorIds} /><div className="flex flex-wrap gap-2"><button type="button" onClick={() => updateLink()} disabled={saving || !selectedLink} className="rounded-2xl bg-activeBlue px-4 py-3 text-sm font-black text-white disabled:opacity-60">Save / 保存</button><button type="button" onClick={() => updateLink('approved')} disabled={saving || !selectedLink} className="rounded-2xl bg-emerald-600 px-4 py-3 text-sm font-black text-white disabled:opacity-60">Approve / 批准</button><button type="button" onClick={() => updateLink('archived')} disabled={saving || !selectedLink} className="rounded-2xl bg-slate-700 px-4 py-3 text-sm font-black text-white disabled:opacity-60">Archive / 归档</button><button type="button" onClick={resetForm} className="rounded-2xl bg-white px-4 py-3 text-sm font-black text-slate-700 ring-1 ring-slate-200">Reset / 重置</button></div></div></SectionCard></div></div>;
}
