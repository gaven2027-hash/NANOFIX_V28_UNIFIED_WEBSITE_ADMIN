import { RegisterShell } from './RegisterShell';

export const metadata = {
  title: 'Register / 注册 | NANOFIX',
  robots: { index: false, follow: false }
};

export default function Page() {
  return <RegisterShell />;
}
